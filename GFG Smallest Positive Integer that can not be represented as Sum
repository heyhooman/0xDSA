Given an array of size N, find the smallest positive integer value that is either not presented
in the array or cannot be represented as a sum(coz sum means you are adding two or more elements) 
of some elements from the array.

class Solution
{   
public:
    long long smallestpositive(vector<long long> array, int n)
    { 
        // code here 
        sort(array.begin(),array.end());
        long long ans=1;
        for(long long i=0;i<n;i++){
            if(array[i]>ans){
                return ans;
            }
            else
            {
                ans+=array[i];
            }
        }
        return ans;
    } 
};
